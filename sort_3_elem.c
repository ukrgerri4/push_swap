#include "push_swap.h"
/*
void sort_3_elem_a(t_dlist *src, t_dlist *dst)
{
    if (FIRST < SECOND && FIRST <= THIRD && SECOND > THIRD) // 1 3 2
    {
        rotate(src);
        swap(src);
        rev_rotate(src);
    }
    else if (FIRST > SECOND && FIRST <= THIRD && SECOND < THIRD) // 2 1 3
        swap(src);
    else if (FIRST <= SECOND && FIRST > THIRD && SECOND > THIRD) // 2 3 1
    {
        rotate(src);
        swap(src);
        rev_rotate(src);
        swap(src);
    }
    else if (FIRST > SECOND && FIRST > THIRD && SECOND > THIRD) // 3 2 1
    {
        swap(src);
        rotate(src);
        swap(src);
        rev_rotate(src);
        swap(src);
    }
    else if (FIRST > SECOND && FIRST > THIRD && SECOND <= THIRD) // 3 1 2
    {
        swap(src);
        rotate(src);
        swap(src);
        rev_rotate(src);
    }
}

void sort_3_elem_b(t_dlist *src, t_dlist *dst)
{
    if (FIRST < SECOND && FIRST <= THIRD && SECOND > THIRD) // 1 3 2
    {
        swap(src);
        rotate(src);
        swap(src);
        rev_rotate(src);
    }
    else if (FIRST > SECOND && FIRST <= THIRD && SECOND < THIRD) // 2 1 3
    {
        rotate(src);
        swap(src);
        rev_rotate(src);
        swap(src);
    }
    else if (FIRST <= SECOND && FIRST > THIRD && SECOND > THIRD) // 2 3 1
        swap(src);
    else if (FIRST < SECOND && FIRST < THIRD && SECOND < THIRD) // 1 2 3
    {
        swap(src);
        rotate(src);
        swap(src);
        rev_rotate(src);
        swap(src);
    }
    else if (FIRST > SECOND && FIRST > THIRD && SECOND <= THIRD) // 3 1 2
    {
        rotate(src);
        swap(src);
        rev_rotate(src);
    }
}
*/